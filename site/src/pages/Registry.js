import React from "react";

const Registry = () => {
  return (
    <div className="registry">
      <h2>Registry</h2>
      <p>Instead of a wedding registry, we will have a card box at the reception.</p>
    </div>
  );
};

export default Registry;
